import React, {useContext} from 'react'
import {Modal} from 'semantic-ui-react'
import { SemestersContext } from '../context/SemestersContext'

export default function ViewSemesterModal() {
    const {} = useContext(SemestersContext)
  return (
    <Modal>

    </Modal>
  )
}
